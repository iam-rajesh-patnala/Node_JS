// Common JS Module Export

// -------- Default export
// const add = (a, b) => a + b;
// module.exports = add;

// -------- Named export
const sub = (a, b) => a - b;
const mul = (a, b) => a * b;

exports.sub = sub;
exports.mul = mul;